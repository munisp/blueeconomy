# Current Implementation State

This record distinguishes **published source and deterministic local validation** from a real deployed, integrated or production-approved capability. No component in this table is represented as live without target-environment evidence.

| Repository | Published implementation | Verified locally | Real target required before live claim |
|---|---|---|---|
| `blueeconomy-contracts` | Versioned Protocol Buffers contracts for common metadata, maritime evidence, waterway safety and immutable audit events. | Contract descriptor validation. | Schema registry/topic and consumer-producer compatibility evidence. |
| `blueeconomy-platform-gitops` | Kubernetes base policy plus reproducible source lock for APISIX, OpenSearch, OpenCTI, Keycloak, Wazuh, Kafka, Temporal, Dapr and CloudNativePG. | Manifest/source-lock validation. | Ministry hybrid cluster, secrets, sizing, storage, network/TLS and approved component configuration. |
| `blueeconomy-security-operations` | Go read-only security endpoint/OIDC verifier with redacted evidence. | Go tests and static analysis. | Approved Keycloak, APISIX, Wazuh/OpenSearch and OpenCTI non-production endpoints/credentials. |
| `blueeconomy-data-platform` | Python bounded file and Kafka ingestion, committed event-envelope validation, append-only Delta Lake insertion, event-ID idempotency, post-persistence Kafka offset commits and secret-safe evidence. | Nine tests; real Apache Kafka 4.3.1 with two consumer groups at offset 1; one Delta row after replay; strict typing/lint/security, hash-locked dependency audits and package builds. | Approved source, TLS/SASL Kafka/Fluvio, object store/IAM/catalog/lineage, authorised records and SRE/recovery evidence. |
| `blueeconomy-maritime-evidence` | Go immutable evidence model and PostgreSQL migrations, including idempotency, row-mutation prevention and one-terminal-decision invariant. | Race-enabled real PostgreSQL integration proves one package, immutable-row trigger and one accepted terminal outcome; measured store coverage contributes to 68.4% package statement coverage. | Approved PostgreSQL/object store, identity/API edge, NSW/PCS/agency adapter and authorised evidence source. |
| `blueeconomy-waterway-safety` | Unsafe-free Rust integrity validator with bounded JSON/base64 input, canonical identifiers, timestamp ordering and real-byte SHA-256 verification. | Eight library and five CLI tests; 87.90% measured line coverage; release/Clippy/OSV checks; complete Rust output → Apache Kafka → governed Delta path with exact payload equality, two committed groups and idempotent replay. | Approved telemetry gateway, device identity/signature protocol, durable sequence state, TLS/SASL broker, rules/geofences, Temporal response workflow and operational owner. |
| `blueeconomy-financial-controls` | Go read-only TigerBeetle verifier built against the official `v0.17.9` client. | Go build/static analysis and fail-closed missing-configuration execution. | Approved TigerBeetle cluster, financial data model, Mojaloop participant/switch, controls and non-production payment evidence. |
| `blueeconomy-credential-verification` | TypeScript issuer/JWKS credential verifier that validates supplied signed JWTs without storing credentials or claims. | Strict TypeScript build and fail-closed configuration tests. | Approved issuer/JWKS, credential profile, holder/relying-party identities and lifecycle policy. |
| `blueeconomy-traceability` | Python bounded real-record provenance validator with a versioned Draft 2020-12 runtime schema, canonical identifiers, chronology/lot continuity and a complete-field chain digest. | Six tests, committed schema validation, strict typing/lint/security checks, hash-locked assurance dependencies, zero known audited dependency findings and package builds. | Authoritative quantity/split/merge/transformation contract, approved harvest/landing/processing/custody systems and authorised partner records. |
| `blueeconomy-administration-service` | Go/PostgreSQL approval-controlled stakeholder onboarding service that uses Keycloak invitations and organization-group activation; it holds onboarding requests/decisions, not user identities. | Real PostgreSQL/Keycloak HTTPS/SMTP lifecycle, denial and idempotency assertions, Go race/static checks and 76.1% integration statement coverage. | Approved Keycloak organization/service account/role-group map, PostgreSQL, APISIX protected routes, reconciliation/revocation, real requesters/approvers and acceptance. |
| `blueeconomy-ministry-portal` | React/OIDC Ministry portal with central stakeholder request form, service-entry directory and real-endpoint probe client. | Production build and strict runtime configuration tests. | Keycloak portal client, APISIX routes, administration endpoint, deployment config and approved user roles. |

## Pending prerequisites

The programme has no authorised endpoint registry, cluster contexts, network routes, credentials, target-side data agreements or partner sandbox configuration attached to this task. Authentic local PostgreSQL, Keycloak, SMTP, Kafka and Delta interactions now exist, but they do not replace agency sources or Ministry environments. The next score-bearing step is Ministry-controlled non-production integration; source will continue to fail closed rather than use assumed endpoints, fabricated agency data, default participants or invented credentials.
