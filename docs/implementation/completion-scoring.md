# Evidence-Based Completion Scoring

## Purpose

This score measures implementation against the Unified Blue Economy Platform specification. It is intentionally **not** a measure of concept design, line count, repository count, mock demonstration or unverified vendor claim. A workstream receives credit only for the strongest evidence actually present.

| Evidence level | Credit within a workstream | Meaning |
|---|---:|---|
| Not started | 0% | No published implementation or test evidence. |
| Source foundation | 25% | Versioned source, contracts, dependency locks and deterministic unit/static validation exist. |
| Local open-source integration | 50% | Real local components interact through authentic protocols and durable stores; no external partner is represented as connected. |
| Ministry non-production integration | 75% | Actual Ministry-controlled integration environment, identity/API edge, observability and security tests have passed. |
| Operational acceptance | 100% | Accountable operational owner has accepted end-to-end workflow, recovery, security/privacy and data/partner evidence. |

## Workstream weights

| Workstream | Weight | Post-implementation evidence | Basis |
|---|---:|---:|---|
| Shared platform, security, operations, data governance and Kubernetes | 15% | 65% | Real authentication hardening, durable external-operation records, source-locked GitOps and Kafka-to-Delta controls exist. No hybrid cluster deployment or Ministry observability evidence exists. |
| S1: Port interoperability and trusted workflow | 15% | 72.5% | Real authenticated PostgreSQL-backed port-call state machine, idempotency/conflict control, optimistic transitions, transactional outbox, document declaration/review/supersession, and clearance decision/amendment workflow exist. A versioned agency-profile registry now binds every new port call to a persisted profile/version; unknown and inactive profiles are rejected, including before clearance. Real PostgreSQL assertions cover registration, active binding, unknown-profile rejection, inactive-profile rejection and inactive-at-clearance rejection. No NSW/PCS/agency adapter is connected. |
| S2: Maritime domain intelligence | 15% | 67.5% | Real maritime-position validation, strict GeoJSON Polygon/MultiPolygon geofence evaluation, authenticated PostgreSQL incident lifecycle, durable spatial-correlation evidence, maker-checker analyst assignment, leased outbox delivery and authentic Kafka acknowledgement/consumption exist. The authorized Ed25519 feed registry supports provider-key rotation with an explicit grace window and revocation, tested against real PostgreSQL. No Ministry-authorized live feed, Sedona/PostGIS deployment, escalation/notification integration or analyst acceptance exists. |
| S3: Maritime finance/CVFF operations | 15% | 67.5% | Real TigerBeetle account/pending/post/void primitives, PostgreSQL financial-intent maker/checker and ambiguity/outbox controls, FSPIOP signing/callback persistence, and live single-replica TigerBeetle lifecycle tests exist. Ledger-observed reconciliation now reads back and records the real ledger condition for `RECONCILIATION_REQUIRED` intents. No production-approved multi-replica ledger deployment, connected participant/switch, settlement authority or regulated operating approval exists. |
| S4: Seafarer credential trust | 10% | 65% | Real JWT/JWKS verification with explicit algorithm allowlisting, protected evidence output, mandatory JTI hash evidence, and an approved-issuer policy that pins issuer, audience, HTTPS JWKS URL, permitted algorithms and an optional canonical KID allowlist. When status enforcement is configured, a verification-only consumer validates every signed status-registry record, issuer, contiguous sequence and signing KID; unknown, suspended or revoked credentials fail closed after JWT verification. No Ministry-approved issuer endpoint, status service, key-rotation authority or credential lifecycle partner exists. |
| S5: Fisheries and aquaculture traceability | 10% | 70% | Runtime schema enforcement, chain digest, custody transitions, recall containment and scope evidence, explicit split/merge genealogy, and canonical quantity/unit conservation exist. Versioned master-data conversion policy is now accepted by the CLI and incorporated in validation evidence, preventing ungoverned cross-unit lineage conversion. No custody source, product/lot master data, authoritative recall issuer or operational recall integration exists. |
| S6: Inland waterway safety telematics | 10% | 72.5% | The Rust validator, official Kafka broker and governed Delta consumer complete an authentic local protocol path with payload equality, two consumer-group commits and idempotent replay. Ordered telemetry emits deterministic payload-free, hash-bound batch evidence and a persisted cursor rejects replay/gap/identity regressions. Versioned safety-rule policies are now deterministically evaluated against every governed policy reference. No approved telemetry gateway, device identity, approved rules engine or safety response workflow is connected. |
| Central administration, Ministry portal and stakeholder onboarding | 10% | 70% | Real PostgreSQL/Keycloak/SMTP lifecycle, verified caller boundary, durable Keycloak side-effect evidence, immutable privacy processing-activity inventory, owner attestation, independent DPO decision workflow, expiry and append-only decision evidence exist. No Ministry Keycloak/APISIX deployment, Ministry DPO approval, NDPC filing/audit evidence or operational acceptance exists. |

## Scoring rule

The current weighted score is the sum of `workstream weight × evidence percentage`. It will be recalculated after each evidence-producing phase. The score cannot increase because a component merely builds; it must satisfy the evidence criteria above.

## Current weighted completion score: **68.63%**

The current calculation is `(15×65 + 15×72.5 + 15×67.5 + 15×67.5 + 10×65 + 10×70 + 10×72.5 + 10×70) ÷ 100 = 68.625%`, rounded to **68.63%**. The increase of **1.875 percentage points** is limited to verified local evidence: S1 versioned agency-profile enforcement (+0.375), S2 provider-key rotation/grace-window enforcement (+0.375), S3 ledger-observed reconciliation (+0.375), S5 versioned conversion-policy evidence (+0.25), and S6 versioned safety-rule evaluation (+0.25). This total was independently recalculated on 2026-08-15 using the published workstream weights and percentages. It reflects the 1.50-point S3 lifecycle increment and corrects arithmetic inconsistencies in prior post-implementation score labels; the listed workstream percentages and this formula are authoritative. Earlier labels of 63.00%, 61.50% and 60.50% were recorded before this arithmetic reconciliation and must not be used as current totals. The evidence increments remain as described, but the authoritative current total is the formula above. the previous 60.00% score remains the historical pre-S6-durable-cursor-store baseline; 58.00% remains the historical pre-S5-recall-scope/S6-cursor baseline; 56.00% remains the historical pre-S5-recall/S6-batch-evidence baseline; 54.75% remains the historical pre-S2-feed/S4-policy baseline; 51.00% remains the historical pre-S1-workflow/S3-live-ledger baseline; 50.25% remains the historical pre-S2-Kafka baseline; 48.25% remains the historical pre-S4-registry baseline; 47.50% remains the historical pre-S2-casework baseline, 46.75% remains the pre-S2 geofence baseline, 44.75% remains the pre-Mojaloop/JTI baseline, 44.00% remains the post-genealogy baseline, 43.50% remains the prior pre-genealogy baseline, and 34.00% remains the prior major baseline. The signed-feed verification, durable provenance record, atomic idempotent incident/outbox creation and authentic Kafka broker delivery are reflected in S2's 65% evidence level. The authenticated PostgreSQL privacy processing-activity inventory, owner attestation, independent DPO decision, expiry and append-only decision evidence are reflected in central administration's 70% evidence level. The S6 atomically persisted stream cursor is reflected in S6's 70% evidence level. The latest 0.50-point increase credits S4 verified active-status enforcement: when configured, signed registry records are validated with an independent public verification key and KID before an ACTIVE status is accepted; unknown, suspended and revoked credentials fail closed. The preceding 0.50-point increase credits S4 manifest-pinned protected-header KID enforcement, verified with real locally signed RS256 JWTs: missing or unapproved KIDs are rejected before remote JWKS resolution. The preceding 0.75-point increase credits the S1 transactionally locked clearance-document prerequisite, verified against real PostgreSQL: an approval before document review is rejected, while clearance after document verification is accepted with the outbox event atomically recorded. The preceding 1.00-point increase credits S5 canonical quantity/unit validation and strict split/merge conservation checks, verified by the strict Python suite. The previous 2.00-point increase credited S5 recall-scope propagation and S6 cross-batch cursor/replay controls. The previous 2.00-point increase credited S5 recalled-lot containment evidence and S6 deterministic batch-evidence tests. The previous 1.25-point increase credited the authenticated local S2 Ed25519 feed-admission evidence and the S4 approved-issuer policy tests. The previous 3.75-point increase credited the verified S1 document/review/clearance workflows and the live single-replica local TigerBeetle side-effect test. The single-replica ledger is development evidence only; it does not represent production quorum, Ministry approval, participant connectivity or regulated settlement authority. It is **not** a production-readiness score, a partner-integration score, or an operational-acceptance score.

## Boundary

The completed local PostgreSQL–Keycloak, PostgreSQL evidence-store, PostgreSQL port-call, PostgreSQL maritime-intelligence casework, PostgreSQL financial-intent, PostgreSQL statement-reconciliation, Kafka-to-Delta, Rust-safety and traceability genealogy tests raise only their stated local evidence. They do not imply that the Ministry’s Keycloak, APISIX, Wazuh, OpenCTI, Kafka, Temporal, PostgreSQL, object store, partners, National Single Window, Mojaloop or TigerBeetle environments have been integrated or accepted.
